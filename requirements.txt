# Optimized requirements for Azure App Service deployment
# Heavy packages (torch, whisper, etc.) removed to reduce size

# Web Framework
Flask==2.3.3
flask-session==0.5.0

# AI/ML Libraries (Core only)
google-generativeai==0.7.2
nltk==3.8.1
scikit-learn==1.3.0
numpy==1.24.3

# String Processing
fuzzywuzzy==0.18.0
python-Levenshtein==0.21.1

# Configuration
python-dotenv==1.0.0

# Utilities
requests==2.29.0

# Firebase Authentication
pyrebase4==4.7.1

# YouTube API Integration
google-api-python-client==2.97.0
google-auth-httplib2==0.1.1
google-auth-oauthlib==1.0.0

# Note: Audio processing packages removed for Azure deployment
# - openai-whisper (very large)
# - torch (very large - ~2GB+)
# - torchaudio (large)
# - pydub (depends on ffmpeg)
# - matplotlib, seaborn, pandas (not essential for core functionality)
# 
# Speech-to-text features will be disabled in production
